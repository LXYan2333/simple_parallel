include(CMakeFindDependencyMacro)

# Same syntax as find_package
find_dependency(MPI REQUIRED)

# Any extra setup

# Add the targets file
include("${CMAKE_CURRENT_LIST_DIR}/simple_parallelTargets.cmake")

if(NOT TARGET simple_parallel::mimalloc)
    find_dependency(mimalloc CONFIG REQUIRED)
endif()

if(NOT TARGET simple_parallel::GSL)
    find_dependency(Microsoft.GSL CONFIG REQUIRED)
endif()